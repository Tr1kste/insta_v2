<% if post.user.id == current_user.id %>
    <div class="<%= class_name %>">
        <%= link_to image_tag('/icons/edit_post.svg'), edit_post_path(post)%>
        <%= link_to image_tag('/icons/delete.svg'), post_path(post), method: :delete, data: { confirm: "Вы уверены, что хотите удалить пост?" } %>
    </div>
<% end %>