<% if user.avatar.attached? %>
    <%= link_to image_tag(user.avatar, class: class_name), user_path(user) %>
<% else %>
    <%= link_to image_tag('/icons/profile.svg', class: class_name), user_path(user) %>
<% end %>